---
import MenuNav from "../components/MenuNav.astro";
import Footer from "../components/Footer.astro";
import ContactForm from "../components/ContactForm.astro";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>XZ</title>
    <meta name="title" content="THANK YOU" />
<meta name="robots" content="noindex, nofollow" />
    <link rel="stylesheet" href="/main.css" />
    <link rel="stylesheet" href="https://use.typekit.net/iza3jks.css" />
    <link
      rel="stylesheet"
      href="//use.fontawesome.com/releases/v6.5.0/css/all.css"
    />
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-SM8RYLZY5G"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-SM8RYLZY5G');
</script>
    <script type="text/javascript">
      // Clarity
      (function (c, l, a, r, i, t, y) {
        c[a] =
          c[a] ||
          function () {
            (c[a].q = c[a].q || []).push(arguments);
          };
        t = l.createElement(r);
        t.async = 1;
        t.src = "https://www.clarity.ms/tag/" + i;
        y = l.getElementsByTagName(r)[0];
        y.parentNode.insertBefore(t, y);
      })(window, document, "clarity", "script", "jovcl27t8k");
    </script>
  </head>
  <body>
   
    
    <section class="vh-90-thankyou">
      <div id="thanks-container">
<img src="img/thankyou-embrace.svg" alt="" style="max-width: 500px;max-height: 200px;margin-bottom: -80px;">        <div id="form-name">
          <form id="name-form" onsubmit="displayWelcome(); return false;" action="https://api.web3forms.com/submit" method="POST">
            <!-- <input type="hidden" name="access_key" value="4b11ec5a-de92-40f5-8959-d81dcb232e08"> -->
            <input type="hidden" name="subject" value="Submission from ThankYou-page">
            <input type="text" name="name" id="name-input" placeholder="Enter your first name">
            <button type="submit" class="btn-closed" disabled>Just do it</button>
          </form>
        </div>
      </div>
      <h1 id="welcome-message">
        <!-- Welcome message will be set here -->
      </h1>
      <div id="thankyouContainer">
<p style="max-width: 800px;font-size: 1.5rem;">Do you know what you are? A curious one. A few weeks ago, we went live with XZ. And you, <span class="name-placeholder"></span>, made difference that day.
 
  <button style="margin: 2rem auto 0rem auto;display: block"><a href="#thankyouSectionTwo" class="btn-red">Stop talking in riddles</a></button>
  
  </p>      </div>
    </section>
    
    <script is:inline>
function displayWelcome() {
  var name = document.getElementById("name-input").value;
  var welcomeMessage = "Hello " + name;
  document.getElementById("welcome-message").textContent = welcomeMessage;

  updateNameInElements(name);

  var form = document.getElementById("form-name");
  form.classList.add("hidden");

  setTimeout(function() {
    form.style.display = 'none';
    var welcome = document.getElementById("welcome-message");
    welcome.style.display = 'flex';
    setTimeout(function() {
      welcome.style.opacity = 1;
      setTimeout(function() {
        welcome.style.marginTop = '-250px'; // Margin change
        setTimeout(function() {
          var thankyou = document.getElementById("thankyouContainer");
          thankyou.style.display = 'flex'; // Or 'flex' if it suits your layout
          thankyou.style.opacity = 1;
        }, 1800);
        setTimeout(function() {
          var thankyouTwo = document.getElementById("thankyouSectionTwo");
          thankyouTwo.style.display = 'flex';
          var thankyouThree = document.getElementById("thankyouSectionThree");
          thankyouThree.style.display = 'flex';
        }, 1800); // Adjust this delay as needed
      }, 1000); // Delay for 1 second for margin change
    }, 100); 
  }, 400); 
}

document.getElementById("name-input").addEventListener("input", function() {
  const nameInput = this;
  const submitButton = document.querySelector("button[type='submit']");

  if (nameInput.value.length >= 3) {
    submitButton.classList.remove("btn-closed");
    submitButton.classList.add("btn-red");
    submitButton.disabled = false; // Enable the button
  } else {
    submitButton.classList.remove("btn-red");
    submitButton.classList.add("btn-closed");
    submitButton.disabled = true; // Keep the button disabled
  }
});

document.getElementById('name-form').addEventListener('submit', function(event) {
  event.preventDefault(); // Prevent the default form submission

  var formData = new FormData(this); // Get form data

  // AJAX request to Web3Forms
  var xhr = new XMLHttpRequest();
  xhr.open('POST', 'https://api.web3forms.com/submit', true);
  xhr.setRequestHeader('Accept', 'application/json'); // Set appropriate header for Web3Forms

  xhr.onreadystatechange = function() {
    if (xhr.readyState === XMLHttpRequest.DONE) {
      var status = xhr.status;
      if (status === 0 || (status >= 200 && status < 400)) {
        // The request has been completed successfully
        displayWelcome(); // Call your custom function after successful submission
      } else {
        // Handle errors here
        console.error('Form submission failed: ', xhr.statusText);
      }
    }
  };

  xhr.send(formData); // Send the form data
});

function updateNameInElements(name) {
  var elementsToUpdate = document.querySelectorAll(".name-placeholder");
  elementsToUpdate.forEach(function(element) {
    element.textContent = name;
  });
}

    </script>
 

     
<section id="thankyouSectionTwo" class="dark-bg" style="display: none;">
  <div class="thankyou-container">
  <h2 class="white">You cared and shared</h2>
  <h3 class="white-color">Your altruistic attitude supporting us, resulted in a new customer for us.
    Sometimes the small actions, become the big actions. And that is why we want to say thank you. <br> Thank you <span class="name-placeholder"></span></h3>!
  
  </div>

    <span class="white-color thankyou-special">The XZ-smileys are just a small appreciation from us, and a reminder that good things happens to good people. You are good. Put the stickers on something fun. </span>
 </section>

<section id="thankyouSectionThree" style="display: none;">


  <div class="thankyou-images">
    <img src="img/thankyou-img/smiley-beer.jpg" alt="">
    <img src="img/thankyou-img/smiley-cat.jpg" alt="">
    <img src="img/thankyou-img/smiley-couple.jpg" alt="">
    <img src="img/thankyou-img/smiley-turning.jpg" alt="">
  </div>

  <a href="https://xzstudios.se">  <div style="display: flex;justify-content: center;margin-top: 8rem;">
  <img src="img/xz-logo-white.svg" alt="" style="max-width: 65px;"></div></a>
</section>

<style>

.vh-90-thankyou {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  height: 100vh;
  text-align: center;
}

.thankyou-images img {
  flex: 0 0 15%; /* Allows the image to grow and shrink, but not beyond its content size */
  object-fit: cover; /* Maintain the aspect ratio of the image */
  max-width: 100%; /* Maximum width is 100% of the container */
  max-height: 400px; /* Maximum height is 100% of the container */
  border-radius: 8px;
  filter: grayscale(100);
  box-shadow: rgba(0, 0, 0, 0.1) 0px 20px 25px -5px, rgba(0, 0, 0, 0.04) 0px 10px 10px -5px;}
/* Media query for mobile screens */
@media (max-width: 768px) { /* Adjust the max-width as needed for your breakpoint */
  .thankyou-images img {
    flex-basis: 40%; /* On mobile, each image takes up half the row */
  }
}
.thankyou-images:hover img {
filter: grayscale (0);
}

#thanks-container {
  transition: filter 1s ease-out;
  filter: blur(0);
}
#thankyouContainer {
  display: none;
  opacity: 0;
  padding: 1.4rem;
  justify-content: center;
  transition: opacity 1s ease-in; /* Transition for fading in */
  position: absolute; /* Make the container absolutely positioned */
  top: 50%; /* Set to 50% of the viewport's height */
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%); /* Adjust for true centering */
  width: 100%; /* Adjust as needed */
  margin-top: 4rem;
  text-align: center;
}
@media(min-width: 768px) {
  text-align: center;
}

#thankyouSectionThree {
  background-color: #c83c4e;
  padding: 4rem 2rem;
}
#form-name {
  transition: opacity 1s ease-out, filter 1s ease-out;
  opacity: 1;
  filter: blur(0);
  padding: 1rem;
}
#form-name button{
  margin-top: 2rem;
}
.thankyou-container {
  max-width: 800px;
}

#form-name.hidden {
  opacity: 0;
  filter: blur(5px);
}

#welcome-message {
  display: none;
  color: #1c1e21;
  padding: 2rem;
  transition: opacity 1s ease-in;
  opacity: 0;
  transition: opacity 1s ease-in, margin-top 1s ease-in-out; /* Added margin-top transition */
  opacity: 0;
  margin-top: 0;
  /* margin-bottom: 4rem; */
  font-size: clamp(2rem, 4rem, 30vw);

}
@media (min-width: 768px) {
  #welcome-message {
    margin-bottom: 0;
    font-size: clamp(2rem, 10rem, 50vw);
  }
  }

#form-name input {
  background-color: white;
  padding: 1rem 2rem;
  border: 1px solid transparent;
  border-radius: 50px;
  width: 100%;
  transition: 0.6s all ease-in-out;
  box-shadow: rgba(0, 0, 0, 0.08) 0px 2px 4px;
  @media (min-width: $breakpoint-tablet) {
    width: 400px;
    &:hover {
      transform: scale(1.02);
    }
  }
}



#thankyouSectionTwo {
  padding: 10rem 1.4rem;
  max-width: 100vw;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  overflow: hidden; /* Add this to contain all inner elements */
}
#thankyouSectionTwo h2 {
  text-align: left;
}
@media (min-width: 768px) {
  #thankyouSectionTwo h2 {
    text-align: center;
  }
}


#thankyouSectionTwo h3 {
  font-weight: 400;
  margin-bottom: 4rem;
  margin-top: 1rem;
  font-size: 1.6rem;
  text-align: center;
}

  .thankyou-images {
  width: 100%; /* Ensure the images container spans the full width */
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  flex-wrap: wrap;
  gap: 1rem;
  margin-bottom: 1rem; /* Add some space at the bottom */
}

.thankyou-images img {
  flex: 0 0 15%; /* Allows the image to grow and shrink, but not beyond its content size */
  object-fit: contain; /* Maintain the aspect ratio of the image */
  max-width: 100%; /* Maximum width is 100% of the container */
  max-height: 400px; /* Maximum height is 100% of the container */
  border-radius: 8px;
  filter: grayscale(100);
  transition: all 0.5s ease;
  box-shadow: rgba(0, 0, 0, 0.1) 0px 20px 25px -5px, rgba(0, 0, 0, 0.04) 0px 10px 10px -5px;}
/* Media query for mobile screens */
@media (max-width: 768px) { /* Adjust the max-width as needed for your breakpoint */
  .thankyou-images img {
    flex-basis: 40%; /* On mobile, each image takes up half the row */
  }
}
.thankyou-images img:hover {
filter: grayscale(0);
transform:scale(1.02);
}

.thankyou-special {
  font-size: 1.3rem;
  max-width: 800px;
  text-align: left;
}
@media (min-width: 768px) {
  .thankyou-special {
    text-align: center;
  }
}


</style>

<ContactForm />
    <script src="/script.js" is:inline></script>
  </body>
</html>