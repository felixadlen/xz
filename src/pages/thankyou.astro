---
import MenuNav from "../components/MenuNav.astro";
import Footer from "../components/Footer.astro";
import ContactForm from "../components/ContactForm.astro";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>XZ</title>
    <meta name="title" content="THANK YOU" />
<meta name="robots" content="noindex, nofollow" />
    <link rel="stylesheet" href="/main.css" />
    <link rel="stylesheet" href="https://use.typekit.net/iza3jks.css" />
    <link
      rel="stylesheet"
      href="//use.fontawesome.com/releases/v6.5.0/css/all.css"
    />
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-SM8RYLZY5G"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-SM8RYLZY5G');
</script>
    <script type="text/javascript">
      // Clarity
      (function (c, l, a, r, i, t, y) {
        c[a] =
          c[a] ||
          function () {
            (c[a].q = c[a].q || []).push(arguments);
          };
        t = l.createElement(r);
        t.async = 1;
        t.src = "https://www.clarity.ms/tag/" + i;
        y = l.getElementsByTagName(r)[0];
        y.parentNode.insertBefore(t, y);
      })(window, document, "clarity", "script", "jovcl27t8k");
    </script>
  </head>
  <body>
   
    
    <section class="vh-90-thankyou">
      <div id="thanks-container">
<img src="/img/thankyou-img/thankyou-embrace.png" id="embraceThanks" alt="red text that says embrace the unknown">       
 <div id="form-name">
          <form id="name-form" onsubmit="displayWelcome(); return false;" action="https://api.web3forms.com/submit" method="POST">
            <!-- <input type="hidden" name="access_key" value="4b11ec5a-de92-40f5-8959-d81dcb232e08"> -->
            <input type="hidden" name="subject" value="Submission from ThankYou-page">
            <input type="text" name="name" id="name-input" placeholder="Enter your first name">
            <button type="submit" class="btn-closed" disabled>Just do it</button>
          </form>
        </div>
      </div>
      <div id="welcome-container">
        <div>
          <h1 id="welcome-message"></h1>
        </div>
        <div id="thankyouContainer">
<p style="max-width: 950px;font-size: 1.5rem;">A few weeks ago we went live with XZ. And you <span class="name-placeholder"></span>, made difference that day.
 
  <button style="margin: 2rem auto 0rem auto;display: block;cursor: pointer;"><a href="#thankyouSectionTwo" class="btn-red" style="cursor: pointer;">What difference?</a></button>
  
  </p>      
</div>
</div>
    </section>
    
    <script is:inline>
      function displayWelcome() {
        window.scrollTo(0, 0); 
        document.body.style.overflow = 'hidden';
        var name = document.getElementById("name-input").value;
        var formattedName = name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();
        var welcomeMessage = "Hello " + formattedName;
        document.getElementById("welcome-message").textContent = welcomeMessage;
    
        updateNameInElements(name);
    
        var form = document.getElementById("form-name");
        form.classList.add("hidden");
        let embraceTextThanks = document.getElementById('embraceThanks');
        embraceTextThanks.classList.add('hidden');
    
        setTimeout(function() {
          var introThanks = document.querySelector('.vh-90-thankyou')
          introThanks.style.height = '100vh';
          form.style.display = 'none';
          embraceTextThanks.style.display = 'none';
          var welcomeContainer = document.getElementById("welcome-container");
        var welcomeMessage = document.getElementById("welcome-message");
        var thankyou = document.getElementById("thankyouContainer");

        welcomeContainer.style.display = 'flex'; // Make sure the container is displayed
        welcomeContainer.style.opacity = 1;
        welcomeMessage.style.display = 'block'; // Change this as needed for your layout
        welcomeMessage.style.opacity = 1; // Make the message fully visible

        thankyou.style.display = 'flex';
        thankyou.style.opacity = 0;
        thankyou.style.transition = "opacity 0.5s ease-in-out 1s";

          setTimeout(function() {
            welcomeContainer.style.marginTop = '-250px'; // Margin change for welcome container
            thankyou.style.opacity = 1;
          }, 1000);
        }, 400);
        setTimeout(function() {
            var thankyouTwo = document.getElementById("thankyouSectionTwo");
            thankyouTwo.style.display = 'flex';
            var thankyouThree = document.getElementById("thankyouSectionThree");
            thankyouThree.style.display = 'flex';
            var thankyouEnding = document.getElementById("thankyou-end-strip");
            thankyouEnding.style.display = 'flex';
            // Unlock scroll here
            document.body.style.overflow = '';
          }, 1800);
      }
    
      document.getElementById("name-input").addEventListener("input", function() {
        const nameInput = this;
        const submitButton = document.querySelector("button[type='submit']");
    
        if (nameInput.value.length >= 3) {
          submitButton.classList.remove("btn-closed");
          submitButton.classList.add("btn-red");
          submitButton.disabled = false;
        } else {
          submitButton.classList.remove("btn-red");
          submitButton.classList.add("btn-closed");
          submitButton.disabled = true;
        }
      });
    
      document.getElementById('name-form').addEventListener('submit', function(event) {
        event.preventDefault();
    
        var formData = new FormData(this);
        var xhr = new XMLHttpRequest();
        xhr.open('POST', 'https://api.web3forms.com/submit', true);
        xhr.setRequestHeader('Accept', 'application/json');
    
        xhr.onreadystatechange = function() {
          if (xhr.readyState === XMLHttpRequest.DONE) {
            var status = xhr.status;
            if (status === 0 || (status >= 200 && status < 400)) {
              displayWelcome();
            } else {
              console.error('Form submission failed: ', xhr.statusText);
            }
          }
        };
    
        xhr.send(formData);
      });
    
      function updateNameInElements(name) {
        var elementsToUpdate = document.querySelectorAll(".name-placeholder");
        elementsToUpdate.forEach(function(element) {
          element.textContent = name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();
        });
      }
    </script>
    

     
<section id="thankyouSectionTwo" class="dark-bg" style="display: none;">
  <div class="thankyou-container">
  <h2 class="white">You cared and shared</h2>
  <h3 class="white-color">Your altruistic attitude in supporting us resulted in a new customer for us that day.
    Sometimes the small actions, become the big actions. And that is why we want to say thank you. Thank you <span class="name-placeholder"></span>!</h3>
  
  </div>

    <span class="white-color thankyou-special">The XZ-smileys are just a small symbol of our appreciation, and a reminder that good things happen to good people. You are good. Put the stickers on something fun. </span>
 </section>

<section id="thankyouSectionThree" style="display: none;">


  <div class="thankyou-images">
    <img src="img/thankyou-img/smiley-beer.jpg" alt="" class="dansk-beer">
    <img src="img/thankyou-img/smiley-cat.jpg" alt="">
    <img src="img/thankyou-img/smiley-couple.jpg" alt="">
    <img src="img/thankyou-img/smiley-turning.jpg" alt="" class="turning-torso">
  </div>

</section>

<section id="thankyou-end-strip">
    <img src="/img/xz-logo-white.svg" alt="" style="max-width: 50px;">
</section>
<style lang="scss">

#welcome-container {
  display: none;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  max-height: 500px;
  width: 100%;
  margin-bottom: -150px;
  min-height: 500px; /* or any other suitable height */
  transition: margin-top 1s ease-in-out; /* Smooth transition for margin-top */
@media (min-width: 768px) {
  margin-top: 4rem;
  margin-bottom: -150px;

  
}
}




.vh-90-thankyou {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 80dvh;
  flex-direction: column;
  // height: 100vh;
  text-align: center;
}

.thankyou-images {
  display: flex;
  flex-wrap: wrap;
  margin: auto;
  justify-content: center;
  gap: 1rem;
  margin-bottom: 1rem;

  img {
    flex: 0 0 100%;
    object-fit: cover;
    max-width: 100%;
    transition: all 0.5s ease-in;
    border-radius: 8px;
    @media (min-width: 768px) {
      flex: 0 0 15%;
      max-height: 400px;
      
    }
    filter: grayscale(100);
    box-shadow: rgba(0, 0, 0, 0.1) 0px 20px 25px -5px, rgba(0, 0, 0, 0.04) 0px 10px 10px -5px;

    &:hover {
      filter: grayscale(0);
      transform: scale(1.02);
    }
  }

  @media (max-width: 768px) {
    img {
      flex-basis: 40%;
    }
  }
}
.turning-torso, .dansk-beer {
  @media (max-width: 768px) {
    display: none;
    
  }
}
#embraceThanks {
  max-width: 300px;max-height: 200px;margin-bottom:0px;
  @media (min-width: 768px) {
    margin-bottom: -20px;
    
  }
}
.thankyou-container {
  max-width: 950px;
}

#thanks-container,
#form-name {
  transition: filter 1s ease-out;
  filter: blur(0);
}


#thankyouContainer {
  display: none;
  opacity: 0;

  margin-top: 1rem;
  padding: 0rem 1.4rem;
  justify-content: center;
  transition: opacity 1s ease-in;

  width: 100%;
  text-align: center;

  @media (min-width: 768px) {
    text-align: center;
  }
}

#thankyouSectionThree {
  background-color: #c83c4e;
  padding: 4rem 2rem;
}

#form-name {
  opacity: 1;
  padding: 1rem;

  &.hidden {
    opacity: 0;
    filter: blur(5px);
  }

  button {
    margin-top: 1rem;
    margin-left: 0rem;
    @media (min-width: 768px) {
      margin-left: 1rem;
      margin-top: 2rem;
    }
  }

  input {
    background-color: white;
    padding: 1rem 2rem;
    border: 1px solid transparent;
    border-radius: 50px;
    width: 100%;
    transition: 0.6s all ease-in-out;
    box-shadow: rgba(0, 0, 0, 0.08) 0px 2px 4px;

    @media (min-width: 768px) {
      width: 400px;

      &:hover {
        transform: scale(1.02);
      }
    }
  }
}


#welcome-message {
  display: none;
  color: #1c1e21;
  padding: 0rem 2rem;
  opacity: 0;
  transition: opacity 1s ease-in, margin-top 1s ease-in-out;
  font-size: clamp(2rem, 4rem, 30vw);

  @media (min-width: 900px) {
    margin-bottom: 2rem;
    font-size: clamp(2rem, 8rem, 50vw);
  }
}

#thankyouSectionTwo {
  padding: 8rem 1.4rem;
  max-width: 100vw;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  @media (min-width: 768px) {
    padding: 16rem 1.4rem;

}
  h2 {
    text-align: left;

    @media (min-width: 768px) {
      text-align: center;
    }
  }

  h3 {
    font-weight: 400;
    margin-bottom: 4rem;
    margin-top: 1rem;
    font-size: 1.6rem;
    text-align: left;
    @media (min-width: 768px) {
      text-align: center;
}
  }
}

.thankyou-special {
  font-size: 1.2rem;
  margin-top: 2rem;
  max-width: 800px;
  text-align: left;

  @media (min-width: 768px) {
    text-align: center;
  }
}

.name-placeholder {
  font-weight: 600;
}
 
#thankyou-end-strip {
  padding: 1rem 2rem 4rem 2rem;
display: none;
  justify-content: center;
  align-items: center;
  background-color: #c83c4e;
}

</style>

<ContactForm />
    <script src="/script.js" is:inline></script>
  </body>
</html>